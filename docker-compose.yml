services:
    mongo_db:
        container_name: mongodb_container
        image: mongo:latest
        ports:
            - '27017:27017'
        volumes:
            - mongo_data:/data/db
        restart: always

volumes:
    mongo_data:
